import { header-Logo } from "../assets/images";
import { hamburger } from "../assets/icons";

const Nav = () => {
  return (
    <header>
      <nav>
        <a href="/">
          <img src="{header-Logo}" />
      </nav>
    </header>
  )
}

export default Nav